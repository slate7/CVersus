{
  "name": "cversus",
  "version": "1.0.0",
  "description": "1-on-1 video calling website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node -e \"require('dotenv').config({quiet:true}); require('./db').migrate().then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "express-session": "^1.19.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "pdfjs-dist": "^4.8.69",
    "pg": "^8.22.0",
    "socket.io": "^4.7.5"
  }
}
